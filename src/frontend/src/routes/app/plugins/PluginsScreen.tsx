import { block } from "million/react";

export const PluginsScreen = block(() => {
	return (
		<article>
			<h1 className="mb-6 text-6xl font-bold">Plugins List</h1>
			<p>Enable or disable plugins</p>
		</article>
	);
});
